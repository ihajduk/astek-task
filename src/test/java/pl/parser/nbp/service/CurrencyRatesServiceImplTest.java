package pl.parser.nbp.service;

import org.junit.Test;

import static org.junit.Assert.*;

public class CurrencyRatesServiceImplTest {

    /**
     * TODO: Should I mock the corresponding components or make acceptance tests for the entire service? Examples:
     * TODO: 1. when parseUtils.parseStringToDate(startDate) thenReturn blabla, when parseString(endDate) thenReturn blabla2, ... etc
     * TODO: 2. result = computeRateStatistics(1,2,3); assertEquals(result, 4);
     * @throws Exception
     */
    @Test
    public void shouldComputeRatesStatistics() throws Exception {
    }
}